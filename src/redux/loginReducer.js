let initialState = {
    userId: null,
    username: null,
    isAuth: false,
    captchaUrl: null
}

const loginReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default loginReducer